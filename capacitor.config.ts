import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aetheria.swdashboard',
  appName: 'aetheria.mobile',
  webDir: 'dist'
};

export default config;
